name: Deployment Workflow



on:

  push:

    branches:

      - main



jobs:

  deploy:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - run: npm install
      - run: git config --global user.email "uditmalhotra98@gmail.com"
      - run: git config --global user.name "Udit Malhotra"
      - run: npm run deploy
